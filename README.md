# AgentRouter Chat

Next.js chat application powered by AgentRouter's OpenAI-compatible API.

## Local setup

```bash
npm install
npm run dev
```

Create `.env.local`:

```env
AGENTROUTER_API_KEY=your_key_here
```

The API key is kept server-side and is never exposed to browser code.
