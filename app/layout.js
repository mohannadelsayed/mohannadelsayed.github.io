export const metadata = { title: "AgentRouter Chat", description: "Chat UI powered by AgentRouter" };
export default function RootLayout({ children }) {
  return <html lang="ar" dir="rtl"><body>{children}</body></html>;
}